# for conda env installation

name: exac_browser
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip:
      - ipython
      - ipykernel
      - pysam
      - numpy

      - pydantic-settings
      - fastapi
      - pymongo
